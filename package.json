{
  "name": "republish-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @republish/api",
    "dev:mcp": "npm run dev -w @republish/mcp-server",
    "dev:mobile": "npm run start -w @republish/mobile",
    "build": "npm run build -w @republish/shared && npm run build -w @republish/feed-ranking && npm run build -w @republish/ai-prompts && npm run build -w @republish/api && npm run build -w @republish/mcp-server",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}
